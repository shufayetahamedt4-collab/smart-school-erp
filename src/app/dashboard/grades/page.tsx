"use client";

import GradingSchemeEditor from "@/components/GradingSchemeEditor";

/** School Admin console → Grading & GPA (PRD §2.1 attendanceMarks). */
export default function DashboardGradesPage() {
  return <GradingSchemeEditor />;
}
