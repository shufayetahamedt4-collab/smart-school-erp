# Firebase App Hosting configuration.
# Docs: https://firebase.google.com/docs/app-hosting

env:
  # Firebase Admin credentials (set via App Hosting secrets in the console,
  # or reference an existing secret):
  - variable: FIREBASE_PROJECT_ID
    secret: FIREBASE_PROJECT_ID
  - variable: FIREBASE_CLIENT_EMAIL
    secret: FIREBASE_CLIENT_EMAIL
  - variable: FIREBASE_PRIVATE_KEY
    secret: FIREBASE_PRIVATE_KEY

  # Auth
  - variable: JWT_SECRET
    secret: JWT_SECRET

  # Firestore database selector — the asia-southeast1 database next to the backend.
  # Migration cutover switch: unset or "(default)" = the africa-south1 database.
  - variable: FIRESTORE_DB_ID
    value: smart-school-db

  # Public origin — used for QR links and storage download URLs.
  # ⚠️ Replace with the real App Hosting URL after the first deploy
  # (e.g. https://smart-school-erp-XXXX.web.app), then with your custom domain.
  - variable: APP_URL
    value: https://smart-school-erp-1--amar-e-school.asia-southeast1.hosted.app

runConfig:
  # One always-warm instance kills cold starts (scale-to-zero reconnection cost
  # was visible as +150–300 ms on the first request after idle).
  minInstances: 1
  maxInstances: 10
  concurrency: 80
  cpu: 1
  memoryMiB: 512
